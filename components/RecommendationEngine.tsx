"use client";

const RecommendationEngine = () => {
    return (
        <div className="flex items-center justify-center h-screen">
            <h1 className="text-4xl font-bold">Recommendation Engine</h1>
        </div>
    );
};

export default RecommendationEngine;